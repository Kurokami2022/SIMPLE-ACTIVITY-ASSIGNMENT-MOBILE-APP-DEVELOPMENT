import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SIMPLE ACTIVITY || ASSIGNMENT || MOBILE APP DEVELOPMENT',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
